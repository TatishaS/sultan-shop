import React, { FC } from "react";
import { useNavigate } from "react-router-dom";

const Product: FC = () => {
  const navigate = useNavigate();

  return <div>Страница продукта</div>;
};

export default Product;
